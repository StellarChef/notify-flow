__pycache__
*.pyc
.env
venv/
.venv/
.vscode/
.idea/
test_man.py
mok_order_1.json
mok_order.json
mok_order_normalsize.json
order_1052.json
order_1057.json
shipping.json
statuses.json

# shop-specific lookup tables - not for the public repo.
# "config/*" rather than "config/": git does not descend into an excluded
# directory, so the negation below would never be reached.
config/*
!config/*.example.json

fetch_things.py

endpoints/API.txt

# runtime logs
logs/

# own security tests, not for the public repo
SECURITY.md
